<%= form_with url: user_search_path(user), method: :get, local: false,
class: "row justify-content-center" do |f| %>
  <%= f.date_field :created_at %>
  <%= f.submit '検索', class: 'btn btn-primary'  %>
<% end %>

<div id="results">
</div>
